<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Decrypt Information</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background-color: #f4f4f4;
        }

        h1 {
            margin-bottom: 20px;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
        }

        textarea {
            padding: 10px;
            width: 300px;
            height: 100px;
            margin-bottom: 10px;
        }

        button {
            padding: 10px;
            background-color: #007bff;
            color: white;
            border: none;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <h1>Decrypt Information</h1>

    <form action="/decrypt" method="post">
        <textarea name="encryptedContent" placeholder="Paste Encrypted Content from the TXT file" required></textarea>
        <textarea name="key" placeholder="Enter Decryption Key" required></textarea>
        <button type="submit">Decrypt</button>
    </form>
</body>
</html>
